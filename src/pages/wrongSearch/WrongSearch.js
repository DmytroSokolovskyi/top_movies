import cl from './WrongSearch.module.css';

export default function WrongSearch () {
    return (
        <div className={cl.divS}>
           <h1 className={cl.wrongSearch}> no results for your request </h1>
        </div>
    );
 }
